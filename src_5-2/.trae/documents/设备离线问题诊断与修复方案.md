
# 设备离线问题诊断与修复方案

## 📋 问题分析

### 当前问题
1. **main.go 中有定时离线任务**：每 30 秒运行一次，检查所有设备的 LatestOnline，如果超过 5 分钟没有更新，就把 DevStatus 设为 0（离线）
2. **payload.go 中 PayloadHandler 已经正确设置了 DevUpdate**：所有功能码（00-06）都设置了 DevStatus = 1 和 LatestOnline = gtime.Now()
3. **mqtt.go 中的 messageHandler**：接收到消息时会更新数据库

### 可能的问题
让我检查代码，找到问题点

---

## 🔍 诊断计划（先执行这个，找到具体原因）

### 步骤1：添加详细的调试日志
修改 `mqtt.go` 和 `payload.go`，添加打印日志：
- 当收到设备消息时，打印设备序列号
- 当 PayloadHandler 处理完成后，打印 DevUpdate 的内容
- 当更新数据库时，打印成功/失败信息

### 步骤2：检查定时任务日志
取消 main.go 中的调试日志注释，打印定时任务运行情况

---

## 🔧 修复计划（如果找到问题后执行）

### 发现的问题（预先分析）
1. **mqtt.go 的 messageHandler**：
   ```go
   if DevUpdate != nil {
       _, err = dao.Dev.Ctx(ctx).OmitEmpty().Where(...).Update()
   }
   ```
   - OmitEmpty() 会忽略零值！但 DevStatus = 1 和 LatestOnline = gtime.Now() 不是空值，应该没问题

2. **让我们检查功能码 03（上发数据配置）和功能码 05（上发数据）是否都正确设置了 DevUpdate**
   - 从代码看，都设置了！

---

## 📋 执行步骤

### 阶段1：添加调试日志（找出问题）
修改文件：
1. `internal/logic/mqtt.go` → 添加详细日志
2. `internal/logic/payload.go` → 添加详细日志
3. `main.go` → 取消定时任务日志注释

### 阶段2：修复发现的问题（待发现）
根据调试日志修复问题

---

## 📊 验收标准
- 设备上报任何消息时，数据库中 LatestOnline 都会更新为当前时间
- 只要设备持续上报，定时任务不会把设备设为离线
- 添加详细的日志，方便排查未来问题
